syntax = "proto3";
package com.vima.recommendation;

import "Shared.proto";

service RecommendationService{
    rpc createUserNod(Uuid) returns (TextMessage);
    rpc createAccomNod(Uuid) returns (TextMessage);
}